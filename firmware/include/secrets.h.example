#pragma once

// Copy this file to secrets.h and fill in the values.
// secrets.h is gitignored - never commit real credentials.

#define WIFI_SSID            ""
#define WIFI_PASSWORD        ""

#define LOCKET_EMAIL         ""
#define LOCKET_PASSWORD      ""

// Public Firebase Web API key shipped with the Locket iOS app.
// Useless without the iOS-spoof headers in locket_auth (see scrapping/.env.example).
#define FIREBASE_API_KEY     ""
