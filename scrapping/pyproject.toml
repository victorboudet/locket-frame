[project]
name = "scrapping"
version = "0.1.0"
description = "Signs in to a Locket account and downloads moment thumbnails"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "python-dotenv>=1.2.2",
]
